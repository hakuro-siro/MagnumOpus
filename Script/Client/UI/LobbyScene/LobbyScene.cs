namespace Script.Client.UI.LobbyScene
{
    public static class LobbyScene
    {
        public enum Buttons
        {
            OpenSettingButton, CloseSettingButton,
            MachingButton, DeckBuildingButton,
            ExitButton
        }

        public enum Pannels
        {
            SettingPannel
        }   
        public enum Texts
        {
            SettingText,
            PannelText
        }
    }
}
